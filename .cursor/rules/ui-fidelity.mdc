---
description: UI 高保真还原规则 — 从 Figma/设计链接生成 Vue 3 页面时，必须像素级匹配设计，完成后运行自测脚本验证 fidelity
globs: src/**/*.vue
alwaysApply: false
---

# UI 高保真还原规则

## 当收到 Figma URL 或 UI 设计还原任务时，必须遵守以下规则：

### 1. 设计分析前置

在写任何代码之前，先从设计稿提取：
- 色彩（主色、辅色、文字色、背景色、边框色）
- 排版（字号层级、字重、行高）
- 间距（padding、margin、栅格）
- 圆角（border-radius）
- 阴影（box-shadow）
- 布局结构（Header / Main / Sidebar / Footer）

### 2. 屏幕适配（1440px+）

- **页面必须设置 `min-width: 1440px`**，保证 1440px 以下不发生布局变形
- 1440px 以上屏幕：内容区使用 `max-width: 1440px; margin: 0 auto;` 居中
- 表格/列表类页面：列宽用 `auto` 弹性扩展，充分利用大屏空间
- 卡片类布局：使用 flex-wrap + gap 自动换行，大屏自动填充
- **禁止**直接写 `width: 1440px`（大屏左侧会留白）— 应使用 `min-width` + `max-width`
- 移动端不需要适配

### 3. 像素级精确

- 所有 `px` 尺寸必须与设计稿**严格一致**，不得四舍五入
- 在 SCSS 中为关键设计值添加 Figma 测量值注释：
  ```scss
  padding: 24px;     /* Figma: 24px */
  border-radius: 8px; /* Figma: 8px */
  ```
- 颜色必须使用设计稿的精确色值，不能"近似色"

### 4. 项目组件优先

优先按以下顺序选用组件：
1. 项目已有的共享组件（`PageContainer`, `SearchContainer`, `TableLayout` 等）
2. Element Plus 基础组件（`el-input`, `el-button`, `el-table` 等）
3. 自己实现（仅在以上都不满足时）

### 5. 自动自测

**必须**在实现后运行 fidelity 自测：

```bash
# 将设计稿截图保存为参考图
# 运行比对
pnpm run verify:fidelity -- --ref=scripts/fidelity/refs/<page>.png --route=/your-route
```

### 6. 差异修复

根据自测报告的差异日志修复，直到：
- 像素差异率 ≤ 1%
- 无结构性差异（缺失元素、布局错位）
- 所有颜色偏差 = 0

### 7. 最终交付物

- 与设计稿高保真匹配的 `.vue` 文件
- 自测通过的差异报告（`scripts/fidelity/reports/`）
