---
description: Vue 3 组件开发约定 — 所有 .vue 文件使用 Composition API + `<script setup lang="ts">`、`defineOptions` 命名、TypeScript Props/Emits
globs: src/**/*.vue
alwaysApply: true
---

# Vue 3 组件开发约定

## 基本原则

- 所有组件必须使用 `<script setup lang="ts">`
- 必须使用 `defineOptions({ name: "ComponentName" })` 显式命名
- Props 优先使用 `withDefaults(defineProps<Interface>(), { ... })` 模式
- Emits 优先使用 TypeScript 调用签名风格

## 代码模板

```vue
<script setup lang="ts">
defineOptions({ name: "ComponentName" })

interface ComponentNameProps {
  title: string
  count?: number
}
const props = withDefaults(defineProps<ComponentNameProps>(), {
  count: 0
})

const emit = defineEmits<{
  (e: "update:modelValue", value: string): void
  (e: "change", value: string | number): void
}>()
</script>
```

## 组件目录结构

可复用组件：`src/components/<PascalCase>/index.vue`
页面组件：`src/views/<feature>/<name>.vue`

## 样式规范

- 使用 `<style lang="scss" scoped>` 限定作用域
- Element Plus 内部样式覆盖使用 `:deep()`
- 优先使用 CSS 变量：`var(--color-primary)`, `var(--bg-color)` 等

## i18n 模板用法

```html
{{ $t("gfuc.some_key" /** 中文注释 **/) }}
```
