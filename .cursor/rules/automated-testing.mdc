---
description: 自动化测试规则 — 新增或修改 src/api、src/store、src/utils、src/hooks 下的代码时，必须同步编写对应的 Vitest 测试
globs: src/{api,store,utils,hooks}/**/*.ts
alwaysApply: false
---

# 自动化测试规则

## 当新增或修改以下层级的代码时，必须同步编写对应的**单元测试**：

### 测试覆盖要求

| 层级 | 测试内容 | 强制程度 |
|------|---------|---------|
| `src/utils/*.ts` | 每个工具函数至少 2 个测试用例（正常 + 边界） | 🔴 必须 |
| `src/api/*.ts` | 每个 API 函数至少测试请求参数结构是否正确 | 🔴 必须 |
| `src/store/*.ts` | 每个 Action 和 Getter 至少 1 个测试用例 | 🟡 推荐 |
| `src/components/**/index.vue` | 共享组件的 Props 渲染和 Emits 触发 | 🟡 推荐 |
| `src/hooks/*.ts` | 核心逻辑路径的测试 | 🟡 推荐 |

### 测试文件命名规则

- 与被测试文件**同级目录**
- 命名格式：`<module>.test.ts`
- 示例：`utils/index.ts` → `utils/index.test.ts`

### 测试编写规范

1. **测试描述使用中文**，格式：`it("场景 → 期望结果")`
2. **三段式结构**：Arrange（准备）→ Act（执行）→ Assert（断言）
3. 工具函数测试：**不需要 Mock**，直接调用函数验证返回值
4. API 层测试：使用 `vi.mock("@/utils/request")` **Mock 请求层**，验证调用参数
5. Store 测试：使用 `@pinia/testing` 的 `createTestingPinia`，为每个测试创建隔离实例
6. 组件测试：使用 `@vue/test-utils` 的 `mount` / `shallowMount`

### 常用的 Mock 模板

```typescript
// API Mock
vi.mock("@/utils/request", () => ({ default: vi.fn() }))

// Store 测试初始化
import { createPinia, setActivePinia } from "pinia"
setActivePinia(createPinia())

// Element Plus/i18n 组件 Mock
const wrapper = mount(Component, {
  global: { mocks: { $t: (key: string) => key } },
})
```

### 运行测试

```bash
# 运行所有测试
pnpm test

# 运行特定文件
pnpm test -- src/utils/index.test.ts

# 运行覆盖率
pnpm test:coverage
```

### 提交前检查

提交代码前确保：
- `pnpm test` 通过（全部测试绿色）
- 新代码的覆盖率不低于项目当前阈值
