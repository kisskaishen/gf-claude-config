---
description: 组件复用规则 — 重复 UI 模块（2次及以上出现）必须封装通用组件，禁止复制粘贴代码，使用项目现有组件优先
globs: src/**/*.{vue,ts}
alwaysApply: true
---

# 组件复用规则

## 核心原则

**同一 UI 模式出现 2 次及以上 → 必须提取为通用组件。禁止复制粘贴代码。**

## 1. 组件复用的判断标准

出现以下任一场景时，必须提取为通用组件：

| 场景 | 示例 | 处理方式 |
|------|------|---------|
| 相同结构出现 2 次以上 | 两处都有"搜索框 + 表格 + 分页" | 使用 `TableLayout` / 或提取新的布局组件 |
| 相同数据结构渲染 | 多处渲染订单状态标签 | 提取 `OrderStatusTag` 组件 |
| 相同交互逻辑 | 多处需要"选择站点 → 刷新列表" | 提取 hook 或基础组件 |
| 相同表单区域 | 多处都有"收件人信息"表单 | 提取 `RecipientForm` 组件 |

## 2. 组件优先级

```
① 项目已有共享组件（PageContainer, TableLayout, SearchContainer...）
② Element Plus 基础组件（el-input, el-button, el-table...）
③ 提取新的通用组件（src/components/<Name>/）
④ 页面局部组件（仅在单页面使用，放在 views/<feature>/components/）
```

## 3. 禁止行为

### ❌ 禁止复制粘贴组件代码

```vue
// ❌ 错误 — 在两处复制了相同的表格代码
// views/order/list.vue
// views/order/exception.vue — 相同的 el-table-column 结构复制粘贴

// ✅ 正确 — 提取为通用组件
// src/components/OrderTable/index.vue
// 两处分别 import 使用
```

### ❌ 禁止在页面文件中定义通用 UI 代码

```typescript
// ❌ 错误 — 在页面内部定义可复用的列配置
const columns = defineColumns([...])   // 如果另一个页面也用相同列，应提取

// ✅ 正确 — 将列配置提取到组件或配置文件中
```

### ❌ 禁止使用深度嵌套的 slot 替代组件提取

```vue
// ⚠️ 如果 #table 中的内容超过 20 行，且在其他页面也出现，应提取组件
<TableLayout>
  <template #table>
    <!-- 20 行以上 el-table-column 配置 → 应提取为独立组件 -->
    <OrderTable />
  </template>
</TableLayout>
```

## 4. 组件复用的边界

### 需要封装的情况

```vue
// ✅ 订单状态标签（多处使用）
<OrderStatusTag :status="order.status" />

// ✅ 站点选择器（多处使用）
<SiteSelect v-model="site" />

// ✅ 文件上传（多处使用）
<CommonUpload :file-list="files" @change="handleUpload" />
```

### 不需要封装的情况

```vue
// ✅ 单次使用的简单结构
<template>
  <el-button type="primary" @click="handleCreate">
    {{ $t("gfuc.create" /** 新建 **/) }}
  </el-button>
</template>
```

## 5. 组件提取步骤

```
① 发现重复
  └─ 同一 UI 模式出现 2 处以上
② 分析差异点
  └─ Props 参数化（标题、数据、回调）
  └─ Slots 自定义（头部、底部、额外操作）
③ 提取组件
  └─ src/components/<通用名>/index.vue
  └─ src/components/<通用名>/types.ts（如有复杂类型）
④ 替换原有代码
  └─ 使用新组件替代所有重复代码
```

## 6. 现有组件速查

在写任何 UI 前，先检查项目中是否已有：

| 场景 | 已有组件 |
|------|---------|
| 页面容器（loading、布局） | `<PageContainer>` |
| 搜索表单（自动布局、折叠、节流） | `<SearchContainer>` |
| 表格（搜索 + 工具栏 + 表格 + 分页） | `<TableLayout>` |
| 空值显示（"-"） | `<ProTableColumn>` |
| 文件上传 | `<CommonUpload>` |
| 反馈弹框 | `<FeedbackDialog>` |
| 成功确认弹框 | `<SuccessDialog>` |
| 站点选择 | `<SiteSelect>` |
| 时区选择 | `<TimezoneSelect>` |
| 语言选择 | `<LangSelect>` |
| SVG 图标 | `<SvgIcon>` |
