---
description: 代码分层解耦规则 — 组件(.vue)、样式(SCSS)、请求(api/)、工具函数(utils/) 强制分离，禁止业务逻辑混写
globs: src/**/*
alwaysApply: true
---

# 代码分层解耦规则

## 强制分层架构

```
src/
├── api/              # ① 请求层 — 只负责 HTTP 请求，不处理业务逻辑
├── store/            # ② 状态层 — 管理全局状态，调用 api 层
├── hooks/            # ③ 组合逻辑层 — 可复用的组合式函数
├── components/       # ④ 组件层 — 纯展示/交互，不直接调用 api
├── views/            # ⑤ 页面层 — 编排组件 + store + hook，轻逻辑
├── utils/            # ⑥ 工具层 — 纯函数，无副作用
└── styles/           # ⑦ 样式层 — 全局样式、变量、覆盖
```

## 禁止行为

### ❌ 禁止在 `.vue` 文件中直接写 API 请求

```typescript
// ❌ 错误 — 业务逻辑混在组件中
<script setup lang="ts">
const list = ref([])
const res = await axios.post("/oms/order/pageList", {})  // ← 禁止
</script>

// ✅ 正确 — 调用 api 层
<script setup lang="ts">
import { getOrderList } from "@/api/order"
const list = ref([])
const res = await getOrderList({})
</script>
```

### ❌ 禁止在组件中直接处理原始 API 响应数据

```typescript
// ❌ 错误 — 组件处理数据转换
const raw = await getList()
const mapped = raw.pageList.map(item => ({ ...item, label: item.name + "(" + item.code + ")" }))

// ✅ 正确 — 在 api 层或 store 层转换
// api/order.ts
export async function getList(params) {
  const res = await request(...)
  return {
    ...res,
    pageList: res.pageList.map(item => formatItem(item)),
  }
}
```

### ❌ 禁止组件直接操作样式逻辑

```vue
// ❌ 错误 — 组件中条件判断写行内样式
<div :style="{ color: status === 1 ? 'green' : 'red' }">

// ✅ 正确 — 用 CSS class 控制
<div :class="['status-tag', status === 1 ? 'active' : 'inactive']">
```

### ❌ 禁止在 api 层处理 UI 逻辑

```typescript
// ❌ 错误 — api 层弹窗
export function deleteOrder(id: string) {
  return request({ url: `/oms/order/${id}`, method: "delete" })
    .then(() => ElMessage.success("删除成功"))  // ← 禁止：UI 逻辑侵入请求层
}

// ✅ 正确 — api 层只返回数据
export function deleteOrder(id: string) {
  return request({ url: `/oms/order/${id}`, method: "delete" })
}
// 组件中处理 UI 反馈
await deleteOrder(id)
ElMessage.success("删除成功")
```

### ❌ 禁止在 styles 中写业务逻辑相关选择器

```scss
// ❌ 错误 — 样式依赖业务状态选择器
.status-1 { color: green; }   // 状态值不应出现在样式文件中
.status-2 { color: red; }

// ✅ 正确 — 样式中只定义抽象状态
.status-active { color: var(--color-success); }
.status-inactive { color: var(--color-danger); }
```

## 各层职责边界

| 层级 | 允许做的事 | 禁止做的事 |
|------|-----------|-----------|
| `api/*.ts` | 发 HTTP 请求、转换请求/响应数据格式 | 操作 DOM、弹 UI 弹窗、修改全局状态 |
| `store/*.ts` | 管理状态、调用 api、派生计算属性 | 操作 DOM、直接操作组件 ref |
| `hooks/*.ts` | 封装可复用的状态+逻辑组合 | 直接操作 DOM、引入组件 |
| `components/*.vue` | 渲染 UI、接收 Props、触发 Emits | 直接调用 api（通过 emit/事件委托给父组件或 store） |
| `views/*.vue` | 编排子组件、连接 store 和 router | 复杂的业务数据转换逻辑 |
| `utils/*.ts` | 纯数据转换、格式化、工具函数 | 发送 HTTP 请求、操作 DOM、使用 Vue API |
| `styles/*.scss` | 全局样式、CSS 变量、Mixin | 业务状态相关选择器 |
