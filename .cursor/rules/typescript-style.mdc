---
description: TypeScript 编码规范 — 禁止滥用 any、使用 `@/` 路径别名、接口命名规范、利用自动导入
globs: src/**/*.ts
alwaysApply: true
---

# TypeScript 编码规范

## 禁止滥用 `any`

除非有充分理由，否则禁止使用 `any`。必要时必须添加注释：
```typescript
// eslint-disable-next-line @typescript-eslint/no-explicit-any
```

## 路径别名

始终使用 `@/` 指向 `src/`：
```typescript
import request from "@/utils/request"
import { useUserStore } from "@/store/user"
```

## 接口命名

- 请求参数：`<Feature><Action>Params` — `OrderListParams`
- 响应数据：`<Feature><Action>Result` — `OrderDetailResult`
- 通用响应使用项目已有的 `Result<T>` / `PageResult<T>`

## 类型推断

优先自动推断，只在初始值为 null/undefined 时显式标注：
```typescript
const count = ref(0)               // ✅ 自动推断
const userInfo = ref<UserInfo | null>(null)  // ✅ 需要标注
```

## 自动导入 API（无需手动 import）

- Vue：`ref`, `computed`, `watch`, `onMounted`, `nextTick` 等
- Vue Router：`useRouter`, `useRoute`
- Pinia：`defineStore`, `storeToRefs`
