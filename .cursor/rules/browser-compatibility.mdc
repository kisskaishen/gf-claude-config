---
description: 浏览器兼容性约束 — 产出前端代码需标注适配的浏览器及最低版本，使用可安全降级的 CSS/JS 特性
globs: src/**/*.{ts,vue,scss}
alwaysApply: true
---

# 浏览器兼容性约束

## 目标浏览器及版本

```yaml
最低兼容:
  Chrome: 90+    (2021年发布)
  Firefox: 88+   (2021年发布)
  Safari: 14+    (2021年发布)
  Edge: 90+      (2021年发布)
  国产浏览器: Chrome 90 兼容模式
  移动端: Safari 14+ / 安卓 WebView 90+
  不兼容: IE 全系列
```

## CSS 兼容性规则

### 使用 Autoprefixer（已配置）

项目已配置 `postcss.config.js` + `autoprefixer`，会自动添加 `-webkit-`、`-moz-` 前缀。编写 CSS 时**写标准语法即可**：

```scss
// ✅ 自动加前缀
.card {
  display: flex;          // → 自动加 -webkit-box
  user-select: none;      // → 自动加 -webkit-user-select
  backdrop-filter: blur(4px); // → 自动加 -webkit-backdrop-filter
}
```

### 注意降级场景

```scss
// ⚠️ backdrop-filter Safari 14+ 支持但需留意性能
// 可以写标准属性，autoprefixer 会自动添加 webkit 前缀

// ⚠️ position: sticky 支持良好（Chrome 56+, Safari 13+）
// 直接使用即可
```

## JavaScript / TypeScript 兼容性规则

### 使用安全的 ES 特性

| 特性 | 兼容性 | 使用建议 |
|------|--------|---------|
| `?.` 可选链 | Chrome 80+, Safari 13.1+ | ✅ 安全使用 |
| `??` 空值合并 | Chrome 80+, Safari 13.1+ | ✅ 安全使用 |
| `||=` `&&=` `??=` | Chrome 85+, Safari 14+ | ✅ 安全使用 |
| `Array.prototype.at()` | Chrome 92+, Safari 15.4+ | ⚠️ 目标浏览器支持，可直接使用 |
| `structuredClone()` | Chrome 98+, Safari 15.4+ | ⚠️ 建议使用 lodash `cloneDeep` 替代 |
| `Top-level await` | Chrome 89+, Safari 15+ | ⚠️ 项目中 `type: "module"`，可在 module 顶层使用 |
| `import.meta.env` | Vite 构建产物已处理 | ✅ 安全使用 |

### 禁止使用的特性

```typescript
// ❌ 禁止 — 兼容性不足
document.querySelector(":has(.foo)")      // CSS :has Safari 15.4+，但动态伪类兼容有限
Element.prototype.scrollIntoViewIfNeeded  // 非标准 API，仅 Chrome 支持

// ❌ 禁止 — 非标准/实验性 API
navigator.userAgentData                   // 仅 Chrome 支持，非标准
PaymentRequest                            // 各浏览器实现不一致
```

## 代码标注规范

### 使用兼容性特性时添加注释

```typescript
// Browser: Chrome 90+, Safari 14+, Firefox 88+
const isSupported = "structuredClone" in window
const data = isSupported ? structuredClone(source) : JSON.parse(JSON.stringify(source))
```

### CSS 降级方案

```scss
// 对于较新的 CSS 特性，提供降级
.card {
  background: #ffffff;                    // 兜底
  background: var(--card-bg, #ffffff);    // CSS 变量
  @supports (backdrop-filter: blur(4px)) {
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(4px);
  }
}
```

## 自测清单

新增页面或组件后，至少在以下浏览器中确认关键功能正常：

- [ ] Chrome 最新版（开发调试，主要环境）
- [ ] 页面在 1440px 视口下布局正常
- [ ] 无 `-webkit-` 前缀遗漏（构建后检查）
- [ ] 未使用上述"禁止使用的特性"
